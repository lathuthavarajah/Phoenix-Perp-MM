# Solana keypair as JSON byte array
# Generate with: solana-keygen new --outfile ~/.config/solana/devnet.json
# Then paste the contents here
SOLANA_KEYPAIR=[1,2,3,...]

# Optional: override log level
RUST_LOG=phoenix_mm=info
